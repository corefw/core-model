/**
 * The 'Resource' namespace provides classes that represent resources, which
 * are instances of models. Most of these classes are used for serialization,
 * especially into JSON API format.
 *
 * @namespace Resource
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );
