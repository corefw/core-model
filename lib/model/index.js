/**
 * The 'Model' namespace provides classes that represent entities/resources,
 * which are usually persisted within a database, and provide a uniform
 * interface for CRUD operations against them.
 *
 * @namespace Model
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );
